# This is a template file - DO NOT commit actual secrets to git
# Create actual secrets using kubectl or Azure Key Vault
# Organization: Edukron Training
# Project: GenAI Engine

apiVersion: v1
kind: Secret
metadata:
  name: genai-engine-secrets
  namespace: production
  labels:
    app: genai-engine
    organization: edukron
type: Opaque
data:
  # Use: echo -n 'your-api-key' | base64
  openai-api-key: BASE64_ENCODED_API_KEY_HERE
  # Additional secrets can be added here
