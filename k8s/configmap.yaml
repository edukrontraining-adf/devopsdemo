apiVersion: v1
kind: ConfigMap
metadata:
  name: genai-app-config
  namespace: default
  labels:
    app: genai-app
data:
  FLASK_ENV: "production"
  LOG_LEVEL: "INFO"
  APP_VERSION: "1.0.0"
  ENVIRONMENT: "production"
